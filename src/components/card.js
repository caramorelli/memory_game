// import React from 'react'
//
// export const Card = (props) => (
//   return (
//     {
//       foundCards.includes(card.name) ? (
//         <img className='card' src={require(`${card.image}`)} alt={card.name}  />
//       ) : (
//         <input type='image' className='card'  src={`${theme}_card.png`} alt={`${card.name}-${idx}`}  />
//       )
//     }
//   )
// )
